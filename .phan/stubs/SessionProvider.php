<?php

namespace MediaWiki\Extension\OAuth;

class SessionProvider {
	public function __toString() {
		// unlike normal stubs, this has an implementation, so it can be reused as a mock in tests
		return self::class;
	}
}
